server:
  port: 9002

spring:
  application:
    name: system

  datasource:
    url: jdbc:mysql://localhost:3306/ssm-oj?useUnicode=true&characterEncoding=utf8&useSSL=true&serverTimezone=GMT%2B8
    username: root
    password: 111111
    hikari:
      minimum-idle: 5 # 最⼩空闲连接数
      maximum-pool-size: 20 # 最⼤连接数
      idle-timeout: 30000 # 空闲连接存活时间（毫秒）
      connection-timeout: 30000 # 连接超时时间（毫秒）

# 关闭自动转成下划线
mybatis-plus:
  configuration:
    map-underscore-to-camel-case: false
